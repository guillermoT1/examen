@model IEnumerable<ProyectoCine.Models.Reservas>


@{
    ViewBag.Title = "Index";
}

<h2>Reservas</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Consumidor.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FechaReserva)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NumeroAsientos)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Consumidor.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FechaReserva)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NumeroAsientos)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ReservasID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ReservasID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ReservasID })
        </td>
    </tr>
}

</table>

