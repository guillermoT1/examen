@model IEnumerable<ProyectoCine.Models.FilaAsientos>

@{
    ViewBag.Title = "Index";
}

<h2>Filas Asientos</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Cines.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Contador_Asientos)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Cines.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Contador_Asientos)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.FilaAsientosID }) |
            @Html.ActionLink("Details", "Details", new { id=item.FilaAsientosID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.FilaAsientosID })
        </td>
    </tr>
}

</table>
